# Git LFS recommendation for forensic samples
# If you choose to use Git LFS, run: git lfs install; git lfs track "samples/*.7z"; git add .gitattributes
samples/*.7z filter=lfs diff=lfs merge=lfs -text
samples/*.mem filter=lfs diff=lfs merge=lfs -text
